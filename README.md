# 🍳 クックノート (cookie)

SNS・ウェブサイトのレシピを保存して、献立提案から買い物メモまでを1つで完結できる料理アプリです。
ビルド不要の静的サイト（HTML/CSS/JavaScript のみ）で、データはブラウザの localStorage に保存されます。

## 機能

- **📥 SNSレシピの文字起こし** — YouTube・Instagram・TikTok の投稿URLを認識し、概要欄・キャプションを貼り付けると「材料」「作り方」「人数」「タグ」に自動で構造化します。
- **🌐 ウェブサイトのインポート** — レシピサイトのURLから構造化データ（schema.org/Recipe の JSON-LD）や本文を解析してレシピを自動抽出します。
- **📖 レシピの保存** — カテゴリ（主菜/副菜/汁物/主食）・ジャンル（和洋中）・タグ付きで保存。検索・編集・人数に応じた分量の自動換算に対応。
- **🍽️ 献立の提案** — 人数・カロリー嗜好（控えめ/標準/しっかり/カスタム）・ジャンルの好み・肉/魚/野菜のこだわりに合わせて、保存済みレシピから主菜+副菜+汁物の組み合わせを提案。食材ベースのカロリー推定DB（約120食材）内蔵。
- **🛒 買い物メモ** — 献立やレシピから材料を人数分に換算して自動集計。同じ食材はまとめて売り場カテゴリ別に整理。チェックリスト・テキストコピーに対応。

## 起動方法

```bash
python3 -m http.server 8080
# → http://localhost:8080 を開く
```

## ファイル構成

| ファイル | 内容 |
| --- | --- |
| `index.html` | 画面（タブ・モーダル） |
| `styles.css` | スタイル |
| `data.js` | 食材カロリーDB・カテゴリ辞書・サンプルレシピ |
| `parser.js` | レシピ文字起こしエンジン（テキスト/JSON-LD解析・分量計算・カロリー推定） |
| `app.js` | アプリ本体（保存・献立提案・買い物リスト） |

初回起動時に12件のサンプルレシピが登録されます（削除可）。

---

## 📈 マーケット・ダッシュボード（おまけ）

`market/` 以下に、主要な株価指数・銘柄の推移をひと目で確認できるダッシュボードを同梱しています（料理アプリとは独立して動作）。

- **主要指標カード** — S&P500・NASDAQ・ダウ・日経平均・ドル円の価格／前日比とミニ推移（スパークライン）。
- **価格チャート** — 期間（1M/3M/6M/1Y/5Y）を切り替えて折れ線＋エリア表示。マウス／タップでクロスヘア表示。
- **指数を比較** — 各指数を「期間の始点=100」で基準化して重ね、相対的な強さを比較。
- **ウォッチリスト** — 指数・主要株（Apple/Microsoft/NVIDIA…）・金/原油/BTC/VIX などを一覧。列ヘッダーで並べ替え。
- **ダーク／ライト対応**・列やカードは配色検証済みパレット準拠。

### データについて
- 価格は **Yahoo Finance** から **CORSプロキシ経由**でブラウザが直接取得します（APIキー不要）。取得結果は10分間ブラウザ内にキャッシュ。
- ネットワークやプロキシの制限で取得できない場合は、**明示的な「デモ値」**（DEMOバッジ・警告バナー付き）を表示します。デモ値は実際の市場価格ではありません。「↻ 更新」で再取得を試せます。

### 起動方法
```bash
python3 -m http.server 8080
# → http://localhost:8080/market/ を開く
```

| ファイル | 内容 |
| --- | --- |
| `market/index.html` | 画面（カード・チャート・表） |
| `market/styles.css` | スタイル（ライト/ダーク・検証済みパレット） |
| `market/data.js` | 銘柄設定・デモデータ生成 |
| `market/app.js` | 取得・キャッシュ・SVGチャート描画 |
